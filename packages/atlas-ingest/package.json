{
  "name": "@GitHub-atlas/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Repository ingestion and metadata extraction for GitHub Atlas",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@GitHub-atlas/types": "workspace:^"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
