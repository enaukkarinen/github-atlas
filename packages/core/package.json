{
  "name": "@github-atlas/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Core functionality for GitHub Atlas",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@github-atlas/graph": "workspace:^"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "tsx": "^4.21.0",
    "typescript": "^5.4.0"
  }
}
