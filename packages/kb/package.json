{
  "name": "@gitHub-atlas/kb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Knowledge base ingestion, chunking, and embedding utilities for GitHub Atlas",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured yet)\"",
    "test": "echo \"(no tests yet)\""
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
